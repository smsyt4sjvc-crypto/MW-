# Raw evidence

Store immutable source artifacts here when licensing and size permit. Otherwise create a dated source-pointer Markdown file containing the title, publisher, publication/data dates, URL, artifact type actually read, and the specific claims used.

Never edit an original artifact. Corrections belong in the wiki and source register.


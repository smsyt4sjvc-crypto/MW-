# MW/$

Agent-native persistent memory for AI-compute economics.

Start with `AGENTS.md`. Canonical facts, claims, methods, entities, sources, and open questions are atomic JSONL records under `memory/`. Retrieve minimal context with `tools/context.py`; validate referential integrity with `tools/validate.py`.

CSV, XLSX, and Markdown are secondary model or narrative projections.
